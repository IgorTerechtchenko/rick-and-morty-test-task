export function isMorty(characterName: string) {
    return characterName.toLowerCase().includes('morty');
}

export function isRick(characterName: string) {
    return characterName.toLowerCase().includes('rick');
}
